

const Title = ({title}) => {
    return (
        <div className="text-center font-semibold text-2xl text-gray-600 my-10 uppercase">
            <h1>{title}</h1>
        </div>
    );
};

export default Title;